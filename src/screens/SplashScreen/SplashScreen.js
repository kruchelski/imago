import React from 'react';
import { View, Text } from 'react-native';
import styles from './style';

const MenuScreen = ({ setLoadingApp }) => {
  return (
    <View style={styles.container}>
      <Text>
        Splash Screen
      </Text>
    </View>
  );
}

export default MenuScreen;